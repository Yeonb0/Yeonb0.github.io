---
categories:
- C-cpp
date: '2026-01-23'
tags:
- C++
- 자료구조
- 문자열
- STL
title: '[C++] String'
toc: true
toc_sticky: true
---

## **1. 개요**

`std::string`은 동적 크기를 가지는 문자열 클래스로, C 스타일 문자열(`char*`)보다 더 안전하고 편리합니다. C++의 `<string>` 헤더에 정의되어 있으며, `std::basic_string<char>`의 타입 정의입니다.

```c++
 #include <string>
 std::string str = "Hello, world!";
```

## **2. 주요 생성자**

## **3. 주요 멤버 함수**

### **🔹 크기 관련**

### **🔹 접근 및 수정**

### **🔹 검색**

### **🔹 비교**

### **🔹 기타**

## **4. 연산자 오버로딩**

```plain text
 +   // 문자열 덧붙이기
 ==, !=, <, >, <=, >= // 비교
 []  // 문자 접근
 <<, >> // 입출력 스트림
```

## **5. 예제 코드**

```c++
 #include <iostream>
 #include <string>
 using namespace std;
 
 int main() {
     string s = "Hello";
     s += " World";
     cout << s << " (length: " << s.length() << ")" << endl;
 
     s.replace(0, 5, "Hi");
     cout << s << endl;
 
     size_t pos = s.find("World");
     if (pos != string::npos)
         cout << "Found at: " << pos << endl;
 
     return 0;
 }
```

## **6. 관련 타입**

- `std::wstring`: 유니코드 지원 (wide character)
- `std::u16string`, `std::u32string`: UTF-16, UTF-32 문자열
- `std::string_view` (C++17): 읽기 전용 문자열 뷰
